= <%=er (var("title").first || pg.title) %>

<%=er (var("description").first || pg.description) %>

== カテゴリー
<% pg.categories(true).each do |cat_pg| %>
* ((<<%=er cat_pg.title %>|<%=er cat_pg.name %>>))
<% end %>

== リンク
<% pg.links(true).each do |link_pg| %>
* ((<<%=er link_pg.title %>|<%=er link_pg.name %>>))
<% end %>
