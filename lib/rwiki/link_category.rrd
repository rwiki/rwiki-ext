<%
	title = var("title").first
	title = pg.title if /\A\s*\z/ =~ title.to_s
%>
= <%=er title %>

<%=er (var("description").first || pg.description) %>

== ¥ê¥ó¥¯
<% pg.categorized_links.each do |link_pg| %>
* ((<<%=er link_pg.title %>|<%=er link_pg.name %>>))
<% end %>
